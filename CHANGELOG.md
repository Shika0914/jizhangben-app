# 记账本 3.1 更新日志

本文件记录 3.1 版本相对于 3.0 版本的全部变化。3.0 版本已于 2026-07-18 冻结，后续产品修改只在 `3.1版本` 文件夹中进行。

## 3.1.0（开发中）

### 版本管理

- 基于当前 3.0 版本建立独立的 3.1 开发版本。
- 3.0 与 3.1 使用不同文件夹保存，避免新修改覆盖可用的旧版本。
- 在网页中加入不可见的 3.1 版本标识，便于部署和问题排查。

### 新增

- 新增信用卡时，如果用户填写了欠款金额，系统会自动为每个有欠款的币种生成一笔本期支出。
- 自动生成的账单使用“其他”支出分类，备注为“初始信用卡欠款”，可在账单和信用卡页面继续查看或修改。
- 多币种信用卡会按币种分别生成初始欠款账单。
- 收入进入信用卡、或转账转入信用卡时，可手动选择计入哪一期信用卡账单。
- 不手动选择账期时，系统会根据交易日期与该卡账单日自动归期。
- 新增多端账本合并：两端新增或修改的是不同内容时，系统会自动合并，不再整本互相覆盖。
- 新增同步冲突提示：同一笔账单或同一项设置在两端都被修改时，会先告知用户，再按最后修改时间保留较新的版本。
- 新增冲突版本备份：用户确认覆盖后，系统会先把原云端账本保存到独立备份表，再写入合并结果。

### 优化

- 新增信用卡时显示欠款入账说明，让用户提前知道填写的金额会如何进入账本。
- 初始欠款会落入当前信用卡账期，即使添加日期已经超过本月账单日，也能在本期账单中看到。
- 信用卡账期与实际交易日期分开保存，手动调整账期不会改变交易发生日期。
- 转入信用卡的还款会显示在对应账期中，并从当期应还合计中扣减。
- 按照 `emil-design-eng` 的设计原则优化 3.1 交互风格：统一按钮按压反馈、聚焦状态、分段控件与导航指示动效。
- 弹窗使用克制的缩放与位移进入效果，Logo 选择菜单从触发位置展开，高频页面切换动效则缩短至 160ms。
- 新增减少动态效果适配，并限制鼠标悬停效果只在支持悬停的设备上增强，改善 iOS 触摸端和键盘操作体验。
- 总览、资产页和统计趋势现在统一默认显示净资产，优先反映扣除信用卡欠款等负余额后的真实财务状况，同时保留总资产切换选项。
- 将钱包的“计入总资产”文案统一为“计入资产统计”，避免在净资产口径下产生误解。
- 删除分类改为历史归档：被删除分类不再出现于新建或编辑账单的选项中，但旧账单仍保留原分类名称、颜色和图标。
- 历史账单筛选中仍可查看已删除分类，并显示“已删除”标记；编辑这类旧账单时，系统会要求用户主动选择当前可用分类。
- 账单、钱包、分类和快速记账模板加入修改时间，作为多端合并时判断新旧版本的统一依据。
- 删除账单、钱包或快速记账模板时保留轻量删除标记，避免另一台设备同步后把已删除内容重新带回来。
- 每次打开或刷新网页时，总览月份自动回到当前月份；进入页面后仍可手动查看其他月份。
- 统计年报的年份改为下拉选择，视觉与周报、月报的周期控件保持一致，并自动列出当前年份及账本涉及的年份。
- 新增独立模拟数据模式，通过 `index.html?demo=1` 打开；覆盖主要账本场景且不会登录或同步到真实 Supabase 账号。
- 统一统计周报、月报和年报的周期选择器，使用一致的尺寸、边框、文字对齐与日历图标，改善 Safari 上的视觉差异。
- 统一总览、账单、信用卡和统计中的月份、周、年份选择控件：横屏与竖屏均采用标签在上、控件在下的布局，并统一为 44px 高度、相同圆角和内边距；移动端自动铺满可用宽度。
- Safari 和 Codex 内置浏览器无法稳定提供原生月份选择面板，因此总览、账单、信用卡账期及统计周期改为真正的网页下拉菜单；月份、年份和周均可直接展开选择，不再需要手动修改数字。
- 时间选择进一步改为级联菜单：年份为第一级，月份为第二级，周报日期为第三级；月份类控件显示“年、月”，周报显示“年、月、日”，避免在单个菜单中罗列大量日期。
- 级联菜单的月份层改为十二宫格：点击月份后以 4 列 × 3 行展示全年 12 个月，当前月份高亮；年份仍为第一级，周报日期仍为最后一级。
- 年份层也改为宫格弹窗，避免传统长下拉菜单；年份与月份的选中态统一改为网站主色青绿色，不再使用棕黄色强调色。
- 记一笔的交易日期、统计周报日期和信用卡分期首期日期统一接入宫格选择：年份、月份、日期依次选择，日期层以带星期标题的日历宫格展示；交易小时与分钟保留为独立下拉控件。
- 修复总览顶部年份、月份宫格在窄屏下被旧版 `overflow: hidden` 样式裁掉的问题，并提高日期弹窗层级，确保宫格可以完整展开和点击。
- 日期宫格改用浏览器原生 `<details>` 展开结构，替代自定义显示/隐藏事件，解决 Safari 正常但 Codex 内置浏览器无法展开总览年份、月份菜单的兼容性差异。
- 更新日期选择器的静态资源版本标识，避免 Codex 内置浏览器沿用旧版脚本缓存，导致 Safari 已生效但内置预览仍无法展开。
- 总览最近账单、账单列表和信用卡账单中的标签改为独立圆角胶囊展示；一笔账含有多个标签时，每个标签分别显示一个椭圆框，备注保持普通文字。
- 总览与账单摘要新增共享的“汇总币种”选择：月支出、月收入、结余、今日支出及账单合计默认只展示一种币种，切换后两处同步更新，避免多币种金额挤满指标卡。
- 汇总币种只改变摘要展示，不换算、不合并不同货币，原始账单金额和币种保持不变。
- 优化多币种钱包卡片：默认突出首要币种余额，并将币种列表收纳为“共 N 种货币”的可展开明细；移动端余额区域独占一行，避免金额、钱包信息与操作按钮互相挤压。
- 优化资产页顶部资产总览卡片：主币种金额单独放大展示，其他币种改为可展开的圆角明细，减少多币种场景下金额并列造成的拥挤感。
- 调整资产总览卡片的横向利用率：多币种明细默认展开，桌面端最多三列展示，减少右侧大面积空白。
- 同步优化总览页净资产卡片：使用主币种大金额与其他币种胶囊明细的同一视觉语言，保持总览与资产页一致。
- 调整资产页资产卡片布局：净资产/总资产切换按钮移动到右上角，资产统计说明移动到底部，减少主金额区域的文字干扰。
- 总览页净资产卡片进一步与资产页资产卡片统一：复用主币种大金额、其他币种展开明细和底部统计说明结构，仅保留右上角“查看资产”入口。
- 统计页资产趋势折线图新增悬浮提示：鼠标移到折线点时显示对应周期与完整金额，键盘聚焦点位时也可查看。
- 统计页新增“消费分析”模块：根据当前统计周期和币种展示资金流向摘要、支出流向排行、标签排行与固定支出识别，帮助用户复盘个人消费结构。
- 统计页重构为子页面结构：新增“概览、消费分析、收入分析、筛选”四个入口，概览保留关键指标与趋势，细化分析拆分到独立子页面。
- 新增收入分类占比图，与支出分类占比使用一致的环形图和排行呈现。
- 新增统计筛选功能：可临时排除分类或标签参与收支统计，筛选只影响统计展示，不修改原始账单。
- 调整统计筛选入口：筛选不再作为顶部大类，而是内嵌到消费分析和收入分析页面中，用户可在当前分析内展开筛选工具。
- 统计筛选按分析类型显示分类：消费分析只展示支出分类，收入分析只展示收入分类，减少无关筛选项干扰。
- 普通资产账户加入负余额保护：微信、支付宝、银行卡、现金和其他钱包不能保存为负余额，也不能通过记账或转账被扣成负数；信用卡仍允许显示欠款负债。
- 收入分析页新增解读模块：展示收入来源摘要、收入来源排行、收入标签排行与稳定收入识别，让收入复盘与消费复盘保持一致。
- 消费分析和收入分析页新增顶部摘要卡片：进入细化分析时先展示本期总额、最大分类、固定/稳定收入支出和标签/笔数等关键指标。
- 消费分析摘要卡片将“支出标签”调整为“支出笔数”；固定支出识别改为更保守规则，交通类支出只有带通勤、月卡、固定等关键词时才计入固定支出。
- 为 3.1 版本加入轻量动效：页面内容分组错峰进入，统计卡片、钱包、账单和模板列表采用短暂上浮显现，桌面端可交互卡片增加微弱悬停反馈；金额内容不做跳动，减少阅读干扰，并适配减少动态效果设置。
- 修复信用卡账单期选择后界面仍显示旧月份的问题；信用卡账单刷新时会同步重绘当前账单期，且只接受有效的“年-月”选择值。
- 进一步修复 Safari 在信用卡账单期从未来年份切回较早年份时被旧状态覆盖的问题；点击年份、月份或日期时先提交新值，并阻止原生详情菜单的默认切换行为。

### 修复

- 修复资产趋势把钱包余额错误带入首笔记录之前的问题；新钱包按建立时间生效，旧版内置钱包按第一笔账单推断余额起点，起点前资产值保持为 0，账单仍从实际发生时间开始影响趋势。
- 修复转账转入信用卡后，余额已变化但信用卡账单页不显示该笔还款的问题。
- 统一记账日期与时区逻辑，修复中国时区凌晨、月初或年初的账单可能被 UTC 时间归入前一天、上个月或上一年的问题。
- 总览今日支出、月份账单、周报、月报、年报、收支图表、信用卡账期和分期日期现在共用同一套本地记账日期规则。
- 修复总览标题显示“总资产”，但实际使用净资产算法的口径不一致问题。
- 修复删除分类后历史账单被批量改为“其他”，导致过往分类统计失真的问题。

### 数据兼容

- 3.1 仍可直接读取 3.0 的本地账本；旧记录缺少修改时间时会自动补齐兼容值，不会改变金额或账单日期。
- Supabase 的 `ledger_states` 新增 `revision` 版本号，并新增仅本人可访问的 `ledger_backups` 冲突备份表；原有账本数据保持不变。
- 3.0 中已经存在的信用卡不会自动补建历史欠款账单，避免升级后产生重复负债；新规则只作用于 3.1 中新建的信用卡。
- 自动生成的初始欠款账单带有可选的来源标记，不影响旧数据读取和云端同步。
- 手动选择的信用卡账期使用可选字段保存；旧账单没有该字段时，仍按原有交易日期与账单日计算，无需数据迁移。
- 新账单同时保存原始时间戳与本地记账日期/时间；旧账单在读取时会自动补齐新字段，不会删除或重建原始记录。
- 已删除分类以可选归档标记保留在原数据中，不需要修改 Supabase 表结构，旧版分类在读取时会自动补齐未归档状态。
